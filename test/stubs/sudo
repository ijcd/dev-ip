#!/usr/bin/env bash
# Fake sudo: log, then drop "sudo" and exec the rest (so `sudo tee X` still writes X).
printf 'sudo %s\n' "$*" >> "${DEVIP_CALL_LOG:-/dev/null}"
[ "$1" = "-n" ] && shift   # non-interactive flag: creds always "cached" in tests
exec "$@"
